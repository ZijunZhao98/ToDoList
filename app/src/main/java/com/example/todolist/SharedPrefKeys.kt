package com.example.todolist

class SharedPrefKeys {
    companion object {
        const val SORT_PREFERENCES_FILE = ".sort_preferences"
        const val sortByTitle = "sort by title"
        const val sortByDeadline = "sort by deadline"
        const val sortByImportance = "sort by importance"
    }
}